packages:
  - apps/*
  - packages/*

# pnpm 12 blocks dependency install scripts by default. Only packages listed here may run them.
# esbuild's script just verifies the platform binary (used by Vite/Vitest and drizzle-kit).
allowBuilds:
  esbuild: true
